# Limitations

- This bundle proves the targeted transient provider fallback metadata test.
- It does not exercise live third-party provider APIs.
- Provider fallback authority remains policy-bound and does not grant merge authority.

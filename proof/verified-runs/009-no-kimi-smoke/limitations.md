# Limitations

- This bundle proves the local no-Kimi smoke gate only.
- It does not prove every provider can execute every role.
- It keeps external MCP preflight disabled to isolate the provider-neutral runtime path.

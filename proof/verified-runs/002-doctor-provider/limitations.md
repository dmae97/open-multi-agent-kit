# Known limitations

- This bundle proves the scoped doctor JSON smoke path only.
- Provider authentication health is environment-dependent and no credential values are captured.
- Output artifacts are sanitized by replacing local paths with [repo-root] or [home].

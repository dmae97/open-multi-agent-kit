# Known limitations

- This bundle proves only the scoped smoke fallback path, not every provider fallback branch.
- The smoke command does not grant merge authority or external API health guarantees.
- Output artifacts are sanitized by replacing local paths with [repo-root] or [home].

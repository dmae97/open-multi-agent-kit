# Known limitations

- This bundle proves only the scoped no-Kimi Codex smoke path.
- Output artifacts are sanitized by replacing the workspace root with `[repo-root]`.
- This is not a full release verification and does not replace `npm run release:rc`.
